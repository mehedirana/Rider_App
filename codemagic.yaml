workflows:
  my-workflow:
    environment:
      node: 16.9.0
      npm: latest
workflows:
  my-workflow:
    scripts:
      - name: Build Android APK
        working_directory: android
        script: ./gradlew assembleRelease
    artifacts:
      - app/build/outputs/**/**/*.apk
